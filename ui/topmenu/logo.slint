
import { Button, HorizontalBox,VerticalBox } from "std-widgets.slint";

export component MyLogo {
    width: 200px;
    height: 50px;
        Image {
            source: @image-url("../../assets/logo.png");
        width: 200px;
        height: 50px;
        }
    }
